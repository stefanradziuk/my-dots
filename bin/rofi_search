#!/bin/bash

# dmenu_search

URL='https://google.com/search?q='
QUERY=$(echo '' | rofi -lines 0 -dmenu -p "google")
if [ -n "$QUERY" ]; then
	xdg-open "${URL}${QUERY}" 2> /dev/null
	exec i3-msg [class="^Chromium$"] focus
fi
